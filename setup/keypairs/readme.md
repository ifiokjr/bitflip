# keypairs

This folder documents keypairs used during testing and development. Private key material must never be committed, even for reproducible fixtures.

Generate disposable local keypairs when a test requires them. Any key that has ever appeared in Git history must be treated as compromised and must not control a deployed program, treasury, mint, or funded account.
