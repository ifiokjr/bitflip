//! This code was AUTOGENERATED by `pina_codama_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::new_without_default,
	clippy::too_many_arguments
)]

#[allow(clippy::len_without_is_empty)]
#[derive(pina::PinaPod)]
#[pinapod(crate = pina::pinapod, no_inherent)]
pub struct SectionState {
	pub discriminator: u8,
	pub migration_version: u8,
	pub owner: solana_pubkey::Pubkey,
	pub asset_id: solana_pubkey::Pubkey,
	pub merkle_tree: solana_pubkey::Pubkey,
	pub bit_vault: solana_pubkey::Pubkey,
	pub game_index: u8,
	pub section_index: u8,
	pub status: u8,
	pub bump: u8,
	pub on_pixels: u16,
	pub leaf_index: u32,
	pub flip_count: u64,
	pub revision: u64,
	pub last_flip_at: i64,
	pub sale_price_lamports: u64,
	pub economy_launched_at: u64,
	pub economy_window_started_at: u64,
	pub economy_last_updated_at: u64,
	pub economy_window_id: u64,
	pub economy_window_target_tokens: u64,
	pub economy_window_rewarded_tokens: u64,
	pub emitted_tokens: u64,
	pub reward_pool_tokens: u64,
	pub controller_price_lamports: u64,
	pub posted_price_lamports: u64,
	pub protocol_fee_lamports: u64,
	pub owner_fee_lamports: u64,
	pub policy_version: u64,
	pub policy_starts_at: i64,
	pub policy_ends_at: i64,
	pub policy_entry_price_tokens: u64,
	pub policy_reward_per_action_tokens: u64,
	pub policy_rules_digest: [u8; 32],
	pub policy_mode: u8,
	pub policy_palette_id: u8,
	pub policy_reward_policy: u8,
	pub pixels: [u8; 512],
}

pub const SECTION_STATE_DISCRIMINATOR: u8 = 3u8;

pub const SECTION_STATE_MIGRATION_VERSION: u8 = 0u8;

impl SectionState {
	pub const LEN: usize = core::mem::size_of::<SectionStateZc>();

	/// Initialize and validate account storage in one pass.
	///
	/// The destination is cleared again if configuration or validation fails.
	pub fn initialize(
		data: &mut [u8],
		configure: impl FnOnce(&mut SectionStateZc),
	) -> Result<&mut SectionStateZc, solana_program_error::ProgramError> {
		<Self as pina::PinaPodFixed>::initialize(data, |account| {
			configure(account);
			account.discriminator = SECTION_STATE_DISCRIMINATOR;
			account.migration_version = SECTION_STATE_MIGRATION_VERSION;
			Ok(())
		})
		.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)
	}

	pub fn from_bytes(data: &[u8]) -> Result<&SectionStateZc, solana_program_error::ProgramError> {
		let account = <Self as pina::PinaPodFixed>::read_exact(data)
			.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)?;
		if account.discriminator != SECTION_STATE_DISCRIMINATOR {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		if account.migration_version != SECTION_STATE_MIGRATION_VERSION {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		Ok(account)
	}

	pub fn from_bytes_mut(
		data: &mut [u8],
	) -> Result<&mut SectionStateZc, solana_program_error::ProgramError> {
		let account = <Self as pina::PinaPodFixed>::read_exact_mut(data)
			.map_err(|_| solana_program_error::ProgramError::InvalidAccountData)?;
		if account.discriminator != SECTION_STATE_DISCRIMINATOR {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		if account.migration_version != SECTION_STATE_MIGRATION_VERSION {
			return Err(solana_program_error::ProgramError::InvalidAccountData);
		}
		Ok(account)
	}
}

impl SectionState {
	pub fn find_pda(game_index: u8, section_index: u8) -> (solana_pubkey::Pubkey, u8) {
		solana_pubkey::Pubkey::find_program_address(
			&[
				"section".as_bytes(),
				&game_index.to_le_bytes(),
				&section_index.to_le_bytes(),
			],
			&crate::BITFLIP_PROGRAM_ID,
		)
	}

	pub fn create_pda(
		game_index: u8,
		section_index: u8,
		bump: u8,
	) -> Result<solana_pubkey::Pubkey, solana_pubkey::PubkeyError> {
		solana_pubkey::Pubkey::create_program_address(
			&[
				"section".as_bytes(),
				&game_index.to_le_bytes(),
				&section_index.to_le_bytes(),
				&[bump],
			],
			&crate::BITFLIP_PROGRAM_ID,
		)
	}
}

/// Whether raw account bytes are stale for this contract: version 0 is this client's current schema, so no bytes are stale.
pub fn section_state_needs_migration(data: &[u8]) -> bool {
	let _ = data;
	false
}

/// Why `SectionState::try_from_bytes` rejected account bytes.
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum SectionStateVersionError {
	/// The bytes do not decode as this account's layout at all.
	InvalidData,
	/// The envelope names this account but the stored version is newer than this client's schema: upgrade this client.
	Future { stored: u8 },
}

impl core::fmt::Display for SectionStateVersionError {
	fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
		match self {
			Self::InvalidData => write!(f, "invalid SectionState account data"),
			Self::Future { stored } => write!(
				f,
				"migration version mismatch: expected 0, received {stored} (the data was written by a newer program; upgrade this client)"
			),
		}
	}
}

impl SectionState {
	/// Decodes current-version bytes and tells stale envelopes (migrate the account) apart from future ones (upgrade this client). The failure message mirrors the generated JavaScript decoder. For the strict current-only convenience returning `ProgramError`, see [`SectionState::from_bytes`].
	pub fn try_from_bytes(data: &[u8]) -> Result<&SectionStateZc, SectionStateVersionError> {
		let account = <Self as pina::PinaPodFixed>::read_exact(data)
			.map_err(|_| SectionStateVersionError::InvalidData)?;
		if account.discriminator != SECTION_STATE_DISCRIMINATOR {
			return Err(SectionStateVersionError::InvalidData);
		}
		if account.migration_version > SECTION_STATE_MIGRATION_VERSION {
			return Err(SectionStateVersionError::Future {
				stored: account.migration_version,
			});
		}
		Ok(account)
	}
}

#[cfg(test)]
mod section_state_version_error_tests {
	use super::*;

	fn envelope(version: u8) -> Vec<u8> {
		let mut data = vec![0_u8; core::mem::size_of::<SectionStateZc>()];
		data[..1].copy_from_slice(&[3]);
		data[1..2].copy_from_slice(&version.to_le_bytes());
		data
	}

	#[test]
	fn stale_and_future_versions_are_distinguishable() {
		let Err(error) = SectionState::try_from_bytes(&envelope(1_u8)) else {
			panic!("a future envelope must fail");
		};
		assert_eq!(error, SectionStateVersionError::Future { stored: 1 });
		assert_eq!(
			SectionStateVersionError::Future { stored: 1 }.to_string(),
			"migration version mismatch: expected 0, received 1 (the data was written by a newer program; upgrade this client)"
		);
		assert!(
			SectionState::try_from_bytes(&envelope(0_u8)).is_ok(),
			"the current version must decode",
		);
	}
}
