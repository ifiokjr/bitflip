/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	combineCodec,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	getStructDecoder,
	getStructEncoder,
	getU8Decoder,
	getU8Encoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlyAccount,
	type ReadonlyUint8Array,
	SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
	SolanaError,
	type TransactionSigner,
	transformEncoder,
	type WritableAccount,
	type WritableSignerAccount,
} from "@solana/kit";
import { getAccountMetaFactory, type ResolvedInstructionAccount } from "@solana/program-client-core";
import { getPinaPodDiscriminatorDecoder, getPinaPodMigrationVersionDecoder } from "../pinaPodCodecs";
import { BITFLIP_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_GAME_DISCRIMINATOR = 4;

export function getInitializeGameDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(INITIALIZE_GAME_DISCRIMINATOR);
}

export const INITIALIZE_GAME_DISCRIMINATOR2 = 0;

export function getInitializeGameDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(INITIALIZE_GAME_DISCRIMINATOR2);
}

export type InitializeGameInstruction<
	TProgram extends string = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
	TAccountPayer extends string | AccountMeta<string> = string,
	TAccountConfig extends string | AccountMeta<string> = string,
	TAccountGame extends string | AccountMeta<string> = string,
	TAccountSection extends string | AccountMeta<string> = string,
	TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
	TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> =
	& Instruction<TProgram>
	& InstructionWithData<ReadonlyUint8Array>
	& InstructionWithAccounts<
		[
			TAccountPayer extends string ? WritableSignerAccount<TAccountPayer> & AccountSignerMeta<TAccountPayer>
				: TAccountPayer,
			TAccountConfig extends string ? WritableAccount<TAccountConfig> : TAccountConfig,
			TAccountGame extends string ? WritableAccount<TAccountGame> : TAccountGame,
			TAccountSection extends string ? WritableAccount<TAccountSection> : TAccountSection,
			TAccountSystemProgram extends string ? ReadonlyAccount<TAccountSystemProgram> : TAccountSystemProgram,
			...TRemainingAccounts,
		]
	>;

export type InitializeGameInstructionData = {
	discriminator: number;
	migrationVersion: number;
	gameIndex: number;
	sectionIndex: number;
	gameBump: number;
	sectionBump: number;
};

export type InitializeGameInstructionDataArgs = {
	gameIndex: number;
	sectionIndex: number;
	gameBump: number;
	sectionBump: number;
};

export function getInitializeGameInstructionDataEncoder(): FixedSizeEncoder<InitializeGameInstructionDataArgs> {
	return transformEncoder(
		getStructEncoder([
			["discriminator", getU8Encoder()],
			["migrationVersion", getU8Encoder()],
			["gameIndex", getU8Encoder()],
			["sectionIndex", getU8Encoder()],
			["gameBump", getU8Encoder()],
			["sectionBump", getU8Encoder()],
		]),
		(value) => ({ ...value, discriminator: 4, migrationVersion: 0 }),
	);
}

export function getInitializeGameInstructionDataDecoder(): FixedSizeDecoder<InitializeGameInstructionData> {
	return getStructDecoder([
		["discriminator", getPinaPodDiscriminatorDecoder(INITIALIZE_GAME_DISCRIMINATOR, getU8Decoder())],
		["migrationVersion", getPinaPodMigrationVersionDecoder(0, getU8Decoder())],
		["gameIndex", getU8Decoder()],
		["sectionIndex", getU8Decoder()],
		["gameBump", getU8Decoder()],
		["sectionBump", getU8Decoder()],
	]);
}

export function getInitializeGameInstructionDataCodec(): FixedSizeCodec<
	InitializeGameInstructionDataArgs,
	InitializeGameInstructionData
> {
	return combineCodec(getInitializeGameInstructionDataEncoder(), getInitializeGameInstructionDataDecoder());
}

export type InitializeGameInput<
	TAccountPayer extends string = string,
	TAccountConfig extends string = string,
	TAccountGame extends string = string,
	TAccountSection extends string = string,
	TAccountSystemProgram extends string = string,
> = {
	payer: TransactionSigner<TAccountPayer>;
	config: Address<TAccountConfig>;
	game: Address<TAccountGame>;
	section: Address<TAccountSection>;
	systemProgram?: Address<TAccountSystemProgram>;
	gameIndex: InitializeGameInstructionDataArgs["gameIndex"];
	sectionIndex: InitializeGameInstructionDataArgs["sectionIndex"];
	gameBump: InitializeGameInstructionDataArgs["gameBump"];
	sectionBump: InitializeGameInstructionDataArgs["sectionBump"];
};

export function getInitializeGameInstruction<
	TAccountPayer extends string,
	TAccountConfig extends string,
	TAccountGame extends string,
	TAccountSection extends string,
	TAccountSystemProgram extends string,
	TProgramAddress extends Address = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
>(
	input: InitializeGameInput<TAccountPayer, TAccountConfig, TAccountGame, TAccountSection, TAccountSystemProgram>,
	config?: { programAddress?: TProgramAddress },
): InitializeGameInstruction<
	TProgramAddress,
	TAccountPayer,
	TAccountConfig,
	TAccountGame,
	TAccountSection,
	TAccountSystemProgram
> {
	// Program address.
	const programAddress = config?.programAddress ?? BITFLIP_PROGRAM_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		payer: { value: input.payer ?? null, isWritable: true },
		config: { value: input.config ?? null, isWritable: true },
		game: { value: input.game ?? null, isWritable: true },
		section: { value: input.section ?? null, isWritable: true },
		systemProgram: { value: input.systemProgram ?? null, isWritable: false },
	};
	const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

	// Original args.
	const args = { ...input };

	// Resolve default values.
	if (!accounts.systemProgram.value) {
		accounts.systemProgram.value = "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
	}

	const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
	return Object.freeze({
		accounts: [
			getAccountMeta("payer", accounts.payer),
			getAccountMeta("config", accounts.config),
			getAccountMeta("game", accounts.game),
			getAccountMeta("section", accounts.section),
			getAccountMeta("systemProgram", accounts.systemProgram),
		],
		data: getInitializeGameInstructionDataEncoder().encode(args as InitializeGameInstructionDataArgs),
		programAddress,
	} as InitializeGameInstruction<
		TProgramAddress,
		TAccountPayer,
		TAccountConfig,
		TAccountGame,
		TAccountSection,
		TAccountSystemProgram
	>);
}

export type ParsedInitializeGameInstruction<
	TProgram extends string = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
	programAddress: Address<TProgram>;
	accounts: {
		payer: TAccountMetas[0];
		config: TAccountMetas[1];
		game: TAccountMetas[2];
		section: TAccountMetas[3];
		systemProgram: TAccountMetas[4];
	};
	data: InitializeGameInstructionData;
};

export function parseInitializeGameInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
	instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeGameInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 5) {
		throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
			actualAccountMetas: instruction.accounts.length,
			expectedAccountMetas: 5,
		});
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: {
			payer: getNextAccount(),
			config: getNextAccount(),
			game: getNextAccount(),
			section: getNextAccount(),
			systemProgram: getNextAccount(),
		},
		data: getInitializeGameInstructionDataDecoder().decode(instruction.data),
	};
}
