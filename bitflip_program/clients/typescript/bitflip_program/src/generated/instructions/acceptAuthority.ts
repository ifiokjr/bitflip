/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	combineCodec,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	getStructDecoder,
	getStructEncoder,
	getU8Decoder,
	getU8Encoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlySignerAccount,
	type ReadonlyUint8Array,
	SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
	SolanaError,
	type TransactionSigner,
	transformEncoder,
	type WritableAccount,
} from "@solana/kit";
import { getAccountMetaFactory, type ResolvedInstructionAccount } from "@solana/program-client-core";
import { getPinaPodDiscriminatorDecoder, getPinaPodMigrationVersionDecoder } from "../pinaPodCodecs";
import { BITFLIP_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const ACCEPT_AUTHORITY_DISCRIMINATOR = 3;

export function getAcceptAuthorityDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(ACCEPT_AUTHORITY_DISCRIMINATOR);
}

export const ACCEPT_AUTHORITY_DISCRIMINATOR2 = 0;

export function getAcceptAuthorityDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(ACCEPT_AUTHORITY_DISCRIMINATOR2);
}

export type AcceptAuthorityInstruction<
	TProgram extends string = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
	TAccountPendingAuthority extends string | AccountMeta<string> = string,
	TAccountConfig extends string | AccountMeta<string> = string,
	TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> =
	& Instruction<TProgram>
	& InstructionWithData<ReadonlyUint8Array>
	& InstructionWithAccounts<
		[
			TAccountPendingAuthority extends string
				? ReadonlySignerAccount<TAccountPendingAuthority> & AccountSignerMeta<TAccountPendingAuthority>
				: TAccountPendingAuthority,
			TAccountConfig extends string ? WritableAccount<TAccountConfig> : TAccountConfig,
			...TRemainingAccounts,
		]
	>;

export type AcceptAuthorityInstructionData = { discriminator: number; migrationVersion: number };

export type AcceptAuthorityInstructionDataArgs = {};

export function getAcceptAuthorityInstructionDataEncoder(): FixedSizeEncoder<AcceptAuthorityInstructionDataArgs> {
	return transformEncoder(
		getStructEncoder([["discriminator", getU8Encoder()], ["migrationVersion", getU8Encoder()]]),
		(value) => ({ ...value, discriminator: 3, migrationVersion: 0 }),
	);
}

export function getAcceptAuthorityInstructionDataDecoder(): FixedSizeDecoder<AcceptAuthorityInstructionData> {
	return getStructDecoder([[
		"discriminator",
		getPinaPodDiscriminatorDecoder(ACCEPT_AUTHORITY_DISCRIMINATOR, getU8Decoder()),
	], ["migrationVersion", getPinaPodMigrationVersionDecoder(0, getU8Decoder())]]);
}

export function getAcceptAuthorityInstructionDataCodec(): FixedSizeCodec<
	AcceptAuthorityInstructionDataArgs,
	AcceptAuthorityInstructionData
> {
	return combineCodec(getAcceptAuthorityInstructionDataEncoder(), getAcceptAuthorityInstructionDataDecoder());
}

export type AcceptAuthorityInput<
	TAccountPendingAuthority extends string = string,
	TAccountConfig extends string = string,
> = {
	pendingAuthority: TransactionSigner<TAccountPendingAuthority>;
	config: Address<TAccountConfig>;
};

export function getAcceptAuthorityInstruction<
	TAccountPendingAuthority extends string,
	TAccountConfig extends string,
	TProgramAddress extends Address = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
>(
	input: AcceptAuthorityInput<TAccountPendingAuthority, TAccountConfig>,
	config?: { programAddress?: TProgramAddress },
): AcceptAuthorityInstruction<TProgramAddress, TAccountPendingAuthority, TAccountConfig> {
	// Program address.
	const programAddress = config?.programAddress ?? BITFLIP_PROGRAM_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		pendingAuthority: { value: input.pendingAuthority ?? null, isWritable: false },
		config: { value: input.config ?? null, isWritable: true },
	};
	const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

	const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
	return Object.freeze(
		{
			accounts: [
				getAccountMeta("pendingAuthority", accounts.pendingAuthority),
				getAccountMeta("config", accounts.config),
			],
			data: getAcceptAuthorityInstructionDataEncoder().encode({}),
			programAddress,
		} as AcceptAuthorityInstruction<TProgramAddress, TAccountPendingAuthority, TAccountConfig>,
	);
}

export type ParsedAcceptAuthorityInstruction<
	TProgram extends string = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
	programAddress: Address<TProgram>;
	accounts: {
		pendingAuthority: TAccountMetas[0];
		config: TAccountMetas[1];
	};
	data: AcceptAuthorityInstructionData;
};

export function parseAcceptAuthorityInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
	instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedAcceptAuthorityInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 2) {
		throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
			actualAccountMetas: instruction.accounts.length,
			expectedAccountMetas: 2,
		});
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: { pendingAuthority: getNextAccount(), config: getNextAccount() },
		data: getAcceptAuthorityInstructionDataDecoder().decode(instruction.data),
	};
}
