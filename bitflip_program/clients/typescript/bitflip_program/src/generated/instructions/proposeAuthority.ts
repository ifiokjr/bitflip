/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	combineCodec,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	getAddressDecoder,
	getAddressEncoder,
	getStructDecoder,
	getStructEncoder,
	getU8Decoder,
	getU8Encoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlySignerAccount,
	type ReadonlyUint8Array,
	SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
	SolanaError,
	type TransactionSigner,
	transformEncoder,
	type WritableAccount,
} from "@solana/kit";
import { getAccountMetaFactory, type ResolvedInstructionAccount } from "@solana/program-client-core";
import { getPinaPodDiscriminatorDecoder, getPinaPodMigrationVersionDecoder } from "../pinaPodCodecs";
import { BITFLIP_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const PROPOSE_AUTHORITY_DISCRIMINATOR = 2;

export function getProposeAuthorityDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(PROPOSE_AUTHORITY_DISCRIMINATOR);
}

export const PROPOSE_AUTHORITY_DISCRIMINATOR2 = 0;

export function getProposeAuthorityDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(PROPOSE_AUTHORITY_DISCRIMINATOR2);
}

export type ProposeAuthorityInstruction<
	TProgram extends string = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
	TAccountAuthority extends string | AccountMeta<string> = string,
	TAccountConfig extends string | AccountMeta<string> = string,
	TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> =
	& Instruction<TProgram>
	& InstructionWithData<ReadonlyUint8Array>
	& InstructionWithAccounts<
		[
			TAccountAuthority extends string ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
				: TAccountAuthority,
			TAccountConfig extends string ? WritableAccount<TAccountConfig> : TAccountConfig,
			...TRemainingAccounts,
		]
	>;

export type ProposeAuthorityInstructionData = {
	discriminator: number;
	migrationVersion: number;
	pendingAuthority: Address;
};

export type ProposeAuthorityInstructionDataArgs = { pendingAuthority: Address };

export function getProposeAuthorityInstructionDataEncoder(): FixedSizeEncoder<ProposeAuthorityInstructionDataArgs> {
	return transformEncoder(
		getStructEncoder([["discriminator", getU8Encoder()], ["migrationVersion", getU8Encoder()], [
			"pendingAuthority",
			getAddressEncoder(),
		]]),
		(value) => ({ ...value, discriminator: 2, migrationVersion: 0 }),
	);
}

export function getProposeAuthorityInstructionDataDecoder(): FixedSizeDecoder<ProposeAuthorityInstructionData> {
	return getStructDecoder([
		["discriminator", getPinaPodDiscriminatorDecoder(PROPOSE_AUTHORITY_DISCRIMINATOR, getU8Decoder())],
		["migrationVersion", getPinaPodMigrationVersionDecoder(0, getU8Decoder())],
		["pendingAuthority", getAddressDecoder()],
	]);
}

export function getProposeAuthorityInstructionDataCodec(): FixedSizeCodec<
	ProposeAuthorityInstructionDataArgs,
	ProposeAuthorityInstructionData
> {
	return combineCodec(getProposeAuthorityInstructionDataEncoder(), getProposeAuthorityInstructionDataDecoder());
}

export type ProposeAuthorityInput<TAccountAuthority extends string = string, TAccountConfig extends string = string> = {
	authority: TransactionSigner<TAccountAuthority>;
	config: Address<TAccountConfig>;
	pendingAuthority: ProposeAuthorityInstructionDataArgs["pendingAuthority"];
};

export function getProposeAuthorityInstruction<
	TAccountAuthority extends string,
	TAccountConfig extends string,
	TProgramAddress extends Address = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
>(
	input: ProposeAuthorityInput<TAccountAuthority, TAccountConfig>,
	config?: { programAddress?: TProgramAddress },
): ProposeAuthorityInstruction<TProgramAddress, TAccountAuthority, TAccountConfig> {
	// Program address.
	const programAddress = config?.programAddress ?? BITFLIP_PROGRAM_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		authority: { value: input.authority ?? null, isWritable: false },
		config: { value: input.config ?? null, isWritable: true },
	};
	const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

	// Original args.
	const args = { ...input };

	const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
	return Object.freeze(
		{
			accounts: [getAccountMeta("authority", accounts.authority), getAccountMeta("config", accounts.config)],
			data: getProposeAuthorityInstructionDataEncoder().encode(args as ProposeAuthorityInstructionDataArgs),
			programAddress,
		} as ProposeAuthorityInstruction<TProgramAddress, TAccountAuthority, TAccountConfig>,
	);
}

export type ParsedProposeAuthorityInstruction<
	TProgram extends string = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
	programAddress: Address<TProgram>;
	accounts: {
		authority: TAccountMetas[0];
		config: TAccountMetas[1];
	};
	data: ProposeAuthorityInstructionData;
};

export function parseProposeAuthorityInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
	instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedProposeAuthorityInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 2) {
		throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
			actualAccountMetas: instruction.accounts.length,
			expectedAccountMetas: 2,
		});
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: { authority: getNextAccount(), config: getNextAccount() },
		data: getProposeAuthorityInstructionDataDecoder().decode(instruction.data),
	};
}
