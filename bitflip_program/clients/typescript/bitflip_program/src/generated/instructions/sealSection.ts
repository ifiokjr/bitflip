/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	combineCodec,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	getStructDecoder,
	getStructEncoder,
	getU8Decoder,
	getU8Encoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlyAccount,
	type ReadonlyUint8Array,
	SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
	SolanaError,
	type TransactionSigner,
	transformEncoder,
	type WritableAccount,
	type WritableSignerAccount,
} from "@solana/kit";
import { getAccountMetaFactory, type ResolvedInstructionAccount } from "@solana/program-client-core";
import { getPinaPodDiscriminatorDecoder, getPinaPodMigrationVersionDecoder } from "../pinaPodCodecs";
import { BITFLIP_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const SEAL_SECTION_DISCRIMINATOR = 7;

export function getSealSectionDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(SEAL_SECTION_DISCRIMINATOR);
}

export const SEAL_SECTION_DISCRIMINATOR2 = 0;

export function getSealSectionDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(SEAL_SECTION_DISCRIMINATOR2);
}

export type SealSectionInstruction<
	TProgram extends string = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
	TAccountOwner extends string | AccountMeta<string> = string,
	TAccountGame extends string | AccountMeta<string> = string,
	TAccountSection extends string | AccountMeta<string> = string,
	TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> =
	& Instruction<TProgram>
	& InstructionWithData<ReadonlyUint8Array>
	& InstructionWithAccounts<
		[
			TAccountOwner extends string ? WritableSignerAccount<TAccountOwner> & AccountSignerMeta<TAccountOwner>
				: TAccountOwner,
			TAccountGame extends string ? ReadonlyAccount<TAccountGame> : TAccountGame,
			TAccountSection extends string ? WritableAccount<TAccountSection> : TAccountSection,
			...TRemainingAccounts,
		]
	>;

export type SealSectionInstructionData = {
	discriminator: number;
	migrationVersion: number;
	gameIndex: number;
	sectionIndex: number;
};

export type SealSectionInstructionDataArgs = { gameIndex: number; sectionIndex: number };

export function getSealSectionInstructionDataEncoder(): FixedSizeEncoder<SealSectionInstructionDataArgs> {
	return transformEncoder(
		getStructEncoder([["discriminator", getU8Encoder()], ["migrationVersion", getU8Encoder()], [
			"gameIndex",
			getU8Encoder(),
		], ["sectionIndex", getU8Encoder()]]),
		(value) => ({ ...value, discriminator: 7, migrationVersion: 0 }),
	);
}

export function getSealSectionInstructionDataDecoder(): FixedSizeDecoder<SealSectionInstructionData> {
	return getStructDecoder([
		["discriminator", getPinaPodDiscriminatorDecoder(SEAL_SECTION_DISCRIMINATOR, getU8Decoder())],
		["migrationVersion", getPinaPodMigrationVersionDecoder(0, getU8Decoder())],
		["gameIndex", getU8Decoder()],
		["sectionIndex", getU8Decoder()],
	]);
}

export function getSealSectionInstructionDataCodec(): FixedSizeCodec<
	SealSectionInstructionDataArgs,
	SealSectionInstructionData
> {
	return combineCodec(getSealSectionInstructionDataEncoder(), getSealSectionInstructionDataDecoder());
}

export type SealSectionInput<
	TAccountOwner extends string = string,
	TAccountGame extends string = string,
	TAccountSection extends string = string,
> = {
	owner: TransactionSigner<TAccountOwner>;
	game: Address<TAccountGame>;
	section: Address<TAccountSection>;
	gameIndex: SealSectionInstructionDataArgs["gameIndex"];
	sectionIndex: SealSectionInstructionDataArgs["sectionIndex"];
};

export function getSealSectionInstruction<
	TAccountOwner extends string,
	TAccountGame extends string,
	TAccountSection extends string,
	TProgramAddress extends Address = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
>(
	input: SealSectionInput<TAccountOwner, TAccountGame, TAccountSection>,
	config?: { programAddress?: TProgramAddress },
): SealSectionInstruction<TProgramAddress, TAccountOwner, TAccountGame, TAccountSection> {
	// Program address.
	const programAddress = config?.programAddress ?? BITFLIP_PROGRAM_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		owner: { value: input.owner ?? null, isWritable: true },
		game: { value: input.game ?? null, isWritable: false },
		section: { value: input.section ?? null, isWritable: true },
	};
	const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

	// Original args.
	const args = { ...input };

	const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
	return Object.freeze(
		{
			accounts: [
				getAccountMeta("owner", accounts.owner),
				getAccountMeta("game", accounts.game),
				getAccountMeta("section", accounts.section),
			],
			data: getSealSectionInstructionDataEncoder().encode(args as SealSectionInstructionDataArgs),
			programAddress,
		} as SealSectionInstruction<TProgramAddress, TAccountOwner, TAccountGame, TAccountSection>,
	);
}

export type ParsedSealSectionInstruction<
	TProgram extends string = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
	programAddress: Address<TProgram>;
	accounts: {
		owner: TAccountMetas[0];
		game: TAccountMetas[1];
		section: TAccountMetas[2];
	};
	data: SealSectionInstructionData;
};

export function parseSealSectionInstruction<TProgram extends string, TAccountMetas extends readonly AccountMeta[]>(
	instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedSealSectionInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 3) {
		throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
			actualAccountMetas: instruction.accounts.length,
			expectedAccountMetas: 3,
		});
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: { owner: getNextAccount(), game: getNextAccount(), section: getNextAccount() },
		data: getSealSectionInstructionDataDecoder().decode(instruction.data),
	};
}
