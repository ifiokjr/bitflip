/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type AccountMeta,
	type AccountSignerMeta,
	type Address,
	combineCodec,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	getStructDecoder,
	getStructEncoder,
	getU8Decoder,
	getU8Encoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlyAccount,
	type ReadonlySignerAccount,
	type ReadonlyUint8Array,
	SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
	SolanaError,
	type TransactionSigner,
	transformEncoder,
	type WritableAccount,
} from "@solana/kit";
import { getAccountMetaFactory, type ResolvedInstructionAccount } from "@solana/program-client-core";
import { getPinaPodDiscriminatorDecoder, getPinaPodMigrationVersionDecoder } from "../pinaPodCodecs";
import { BITFLIP_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const CONFIGURE_BIT_CUSTODY_DISCRIMINATOR = 13;

export function getConfigureBitCustodyDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(CONFIGURE_BIT_CUSTODY_DISCRIMINATOR);
}

export const CONFIGURE_BIT_CUSTODY_DISCRIMINATOR2 = 0;

export function getConfigureBitCustodyDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(CONFIGURE_BIT_CUSTODY_DISCRIMINATOR2);
}

export type ConfigureBitCustodyInstruction<
	TProgram extends string = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
	TAccountAuthority extends string | AccountMeta<string> = string,
	TAccountConfig extends string | AccountMeta<string> = string,
	TAccountBitMint extends string | AccountMeta<string> = string,
	TAccountBitReserve extends string | AccountMeta<string> = string,
	TAccountTokenProgram extends string | AccountMeta<string> = "TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb",
	TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> =
	& Instruction<TProgram>
	& InstructionWithData<ReadonlyUint8Array>
	& InstructionWithAccounts<
		[
			TAccountAuthority extends string ? ReadonlySignerAccount<TAccountAuthority> & AccountSignerMeta<TAccountAuthority>
				: TAccountAuthority,
			TAccountConfig extends string ? WritableAccount<TAccountConfig> : TAccountConfig,
			TAccountBitMint extends string ? ReadonlyAccount<TAccountBitMint> : TAccountBitMint,
			TAccountBitReserve extends string ? ReadonlyAccount<TAccountBitReserve> : TAccountBitReserve,
			TAccountTokenProgram extends string ? ReadonlyAccount<TAccountTokenProgram> : TAccountTokenProgram,
			...TRemainingAccounts,
		]
	>;

export type ConfigureBitCustodyInstructionData = { discriminator: number; migrationVersion: number };

export type ConfigureBitCustodyInstructionDataArgs = {};

export function getConfigureBitCustodyInstructionDataEncoder(): FixedSizeEncoder<
	ConfigureBitCustodyInstructionDataArgs
> {
	return transformEncoder(
		getStructEncoder([["discriminator", getU8Encoder()], ["migrationVersion", getU8Encoder()]]),
		(value) => ({ ...value, discriminator: 13, migrationVersion: 0 }),
	);
}

export function getConfigureBitCustodyInstructionDataDecoder(): FixedSizeDecoder<ConfigureBitCustodyInstructionData> {
	return getStructDecoder([[
		"discriminator",
		getPinaPodDiscriminatorDecoder(CONFIGURE_BIT_CUSTODY_DISCRIMINATOR, getU8Decoder()),
	], ["migrationVersion", getPinaPodMigrationVersionDecoder(0, getU8Decoder())]]);
}

export function getConfigureBitCustodyInstructionDataCodec(): FixedSizeCodec<
	ConfigureBitCustodyInstructionDataArgs,
	ConfigureBitCustodyInstructionData
> {
	return combineCodec(getConfigureBitCustodyInstructionDataEncoder(), getConfigureBitCustodyInstructionDataDecoder());
}

export type ConfigureBitCustodyInput<
	TAccountAuthority extends string = string,
	TAccountConfig extends string = string,
	TAccountBitMint extends string = string,
	TAccountBitReserve extends string = string,
	TAccountTokenProgram extends string = string,
> = {
	authority: TransactionSigner<TAccountAuthority>;
	config: Address<TAccountConfig>;
	bitMint: Address<TAccountBitMint>;
	bitReserve: Address<TAccountBitReserve>;
	tokenProgram?: Address<TAccountTokenProgram>;
};

export function getConfigureBitCustodyInstruction<
	TAccountAuthority extends string,
	TAccountConfig extends string,
	TAccountBitMint extends string,
	TAccountBitReserve extends string,
	TAccountTokenProgram extends string,
	TProgramAddress extends Address = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
>(
	input: ConfigureBitCustodyInput<
		TAccountAuthority,
		TAccountConfig,
		TAccountBitMint,
		TAccountBitReserve,
		TAccountTokenProgram
	>,
	config?: { programAddress?: TProgramAddress },
): ConfigureBitCustodyInstruction<
	TProgramAddress,
	TAccountAuthority,
	TAccountConfig,
	TAccountBitMint,
	TAccountBitReserve,
	TAccountTokenProgram
> {
	// Program address.
	const programAddress = config?.programAddress ?? BITFLIP_PROGRAM_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		authority: { value: input.authority ?? null, isWritable: false },
		config: { value: input.config ?? null, isWritable: true },
		bitMint: { value: input.bitMint ?? null, isWritable: false },
		bitReserve: { value: input.bitReserve ?? null, isWritable: false },
		tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
	};
	const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

	// Resolve default values.
	if (!accounts.tokenProgram.value) {
		accounts.tokenProgram.value = "TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb" as Address<
			"TokenzQdBNbLqP5VEhdkAS6EPFLC1PHnBqCXEpPxuEb"
		>;
	}

	const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
	return Object.freeze({
		accounts: [
			getAccountMeta("authority", accounts.authority),
			getAccountMeta("config", accounts.config),
			getAccountMeta("bitMint", accounts.bitMint),
			getAccountMeta("bitReserve", accounts.bitReserve),
			getAccountMeta("tokenProgram", accounts.tokenProgram),
		],
		data: getConfigureBitCustodyInstructionDataEncoder().encode({}),
		programAddress,
	} as ConfigureBitCustodyInstruction<
		TProgramAddress,
		TAccountAuthority,
		TAccountConfig,
		TAccountBitMint,
		TAccountBitReserve,
		TAccountTokenProgram
	>);
}

export type ParsedConfigureBitCustodyInstruction<
	TProgram extends string = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
	programAddress: Address<TProgram>;
	accounts: {
		authority: TAccountMetas[0];
		config: TAccountMetas[1];
		bitMint: TAccountMetas[2];
		bitReserve: TAccountMetas[3];
		tokenProgram: TAccountMetas[4];
	};
	data: ConfigureBitCustodyInstructionData;
};

export function parseConfigureBitCustodyInstruction<
	TProgram extends string,
	TAccountMetas extends readonly AccountMeta[],
>(
	instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedConfigureBitCustodyInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 5) {
		throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
			actualAccountMetas: instruction.accounts.length,
			expectedAccountMetas: 5,
		});
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: {
			authority: getNextAccount(),
			config: getNextAccount(),
			bitMint: getNextAccount(),
			bitReserve: getNextAccount(),
			tokenProgram: getNextAccount(),
		},
		data: getConfigureBitCustodyInstructionDataDecoder().decode(instruction.data),
	};
}
