/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type AccountMeta,
	type Address,
	combineCodec,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	getStructDecoder,
	getStructEncoder,
	getU8Decoder,
	getU8Encoder,
	type Instruction,
	type InstructionWithAccounts,
	type InstructionWithData,
	type ReadonlyAccount,
	type ReadonlyUint8Array,
	SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
	SolanaError,
	transformEncoder,
	type WritableAccount,
} from "@solana/kit";
import { getAccountMetaFactory, type ResolvedInstructionAccount } from "@solana/program-client-core";
import { getPinaPodDiscriminatorDecoder, getPinaPodMigrationVersionDecoder } from "../pinaPodCodecs";
import { BITFLIP_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const SETTLE_SECTION_ECONOMY_DISCRIMINATOR = 12;

export function getSettleSectionEconomyDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(SETTLE_SECTION_ECONOMY_DISCRIMINATOR);
}

export const SETTLE_SECTION_ECONOMY_DISCRIMINATOR2 = 0;

export function getSettleSectionEconomyDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(SETTLE_SECTION_ECONOMY_DISCRIMINATOR2);
}

export type SettleSectionEconomyInstruction<
	TProgram extends string = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
	TAccountGame extends string | AccountMeta<string> = string,
	TAccountSection extends string | AccountMeta<string> = string,
	TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> =
	& Instruction<TProgram>
	& InstructionWithData<ReadonlyUint8Array>
	& InstructionWithAccounts<
		[
			TAccountGame extends string ? ReadonlyAccount<TAccountGame> : TAccountGame,
			TAccountSection extends string ? WritableAccount<TAccountSection> : TAccountSection,
			...TRemainingAccounts,
		]
	>;

export type SettleSectionEconomyInstructionData = {
	discriminator: number;
	migrationVersion: number;
	gameIndex: number;
	sectionIndex: number;
};

export type SettleSectionEconomyInstructionDataArgs = { gameIndex: number; sectionIndex: number };

export function getSettleSectionEconomyInstructionDataEncoder(): FixedSizeEncoder<
	SettleSectionEconomyInstructionDataArgs
> {
	return transformEncoder(
		getStructEncoder([["discriminator", getU8Encoder()], ["migrationVersion", getU8Encoder()], [
			"gameIndex",
			getU8Encoder(),
		], ["sectionIndex", getU8Encoder()]]),
		(value) => ({ ...value, discriminator: 12, migrationVersion: 0 }),
	);
}

export function getSettleSectionEconomyInstructionDataDecoder(): FixedSizeDecoder<SettleSectionEconomyInstructionData> {
	return getStructDecoder([
		["discriminator", getPinaPodDiscriminatorDecoder(SETTLE_SECTION_ECONOMY_DISCRIMINATOR, getU8Decoder())],
		["migrationVersion", getPinaPodMigrationVersionDecoder(0, getU8Decoder())],
		["gameIndex", getU8Decoder()],
		["sectionIndex", getU8Decoder()],
	]);
}

export function getSettleSectionEconomyInstructionDataCodec(): FixedSizeCodec<
	SettleSectionEconomyInstructionDataArgs,
	SettleSectionEconomyInstructionData
> {
	return combineCodec(getSettleSectionEconomyInstructionDataEncoder(), getSettleSectionEconomyInstructionDataDecoder());
}

export type SettleSectionEconomyInput<TAccountGame extends string = string, TAccountSection extends string = string> = {
	game: Address<TAccountGame>;
	section: Address<TAccountSection>;
	gameIndex: SettleSectionEconomyInstructionDataArgs["gameIndex"];
	sectionIndex: SettleSectionEconomyInstructionDataArgs["sectionIndex"];
};

export function getSettleSectionEconomyInstruction<
	TAccountGame extends string,
	TAccountSection extends string,
	TProgramAddress extends Address = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
>(
	input: SettleSectionEconomyInput<TAccountGame, TAccountSection>,
	config?: { programAddress?: TProgramAddress },
): SettleSectionEconomyInstruction<TProgramAddress, TAccountGame, TAccountSection> {
	// Program address.
	const programAddress = config?.programAddress ?? BITFLIP_PROGRAM_PROGRAM_ADDRESS;

	// Original accounts.
	const originalAccounts = {
		game: { value: input.game ?? null, isWritable: false },
		section: { value: input.section ?? null, isWritable: true },
	};
	const accounts = originalAccounts as Record<keyof typeof originalAccounts, ResolvedInstructionAccount>;

	// Original args.
	const args = { ...input };

	const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
	return Object.freeze(
		{
			accounts: [getAccountMeta("game", accounts.game), getAccountMeta("section", accounts.section)],
			data: getSettleSectionEconomyInstructionDataEncoder().encode(args as SettleSectionEconomyInstructionDataArgs),
			programAddress,
		} as SettleSectionEconomyInstruction<TProgramAddress, TAccountGame, TAccountSection>,
	);
}

export type ParsedSettleSectionEconomyInstruction<
	TProgram extends string = typeof BITFLIP_PROGRAM_PROGRAM_ADDRESS,
	TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
	programAddress: Address<TProgram>;
	accounts: {
		game: TAccountMetas[0];
		section: TAccountMetas[1];
	};
	data: SettleSectionEconomyInstructionData;
};

export function parseSettleSectionEconomyInstruction<
	TProgram extends string,
	TAccountMetas extends readonly AccountMeta[],
>(
	instruction: Instruction<TProgram> & InstructionWithAccounts<TAccountMetas> & InstructionWithData<ReadonlyUint8Array>,
): ParsedSettleSectionEconomyInstruction<TProgram, TAccountMetas> {
	if (instruction.accounts.length < 2) {
		throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
			actualAccountMetas: instruction.accounts.length,
			expectedAccountMetas: 2,
		});
	}
	let accountIndex = 0;
	const getNextAccount = () => {
		const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
		accountIndex += 1;
		return accountMeta;
	};
	return {
		programAddress: instruction.programAddress,
		accounts: { game: getNextAccount(), section: getNextAccount() },
		data: getSettleSectionEconomyInstructionDataDecoder().decode(instruction.data),
	};
}
