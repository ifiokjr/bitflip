/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type Account,
	type Address,
	assertAccountExists,
	assertAccountsExist,
	combineCodec,
	decodeAccount,
	type EncodedAccount,
	type FetchAccountConfig,
	type FetchAccountsConfig,
	fetchEncodedAccount,
	fetchEncodedAccounts,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	getI64Decoder,
	getI64Encoder,
	getStructDecoder,
	getStructEncoder,
	getU16Decoder,
	getU16Encoder,
	getU64Decoder,
	getU64Encoder,
	getU8Decoder,
	getU8Encoder,
	type MaybeAccount,
	type MaybeEncodedAccount,
	type ReadonlyUint8Array,
	transformEncoder,
} from "@solana/kit";
import { findGamePda, GameSeeds } from "../pdas";
import { getPinaPodDiscriminatorDecoder, getPinaPodMigrationVersionDecoder } from "../pinaPodCodecs";

export const GAME_STATE_DISCRIMINATOR = 2;

export function getGameStateDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(GAME_STATE_DISCRIMINATOR);
}

export const GAME_STATE_DISCRIMINATOR2 = 0;

export function getGameStateDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(GAME_STATE_DISCRIMINATOR2);
}

export type GameState = {
	discriminator: number;
	migrationVersion: number;
	gameIndex: number;
	status: number;
	bump: number;
	economyVersion: number;
	startsAt: bigint;
	nextSection: number;
	mintedSections: number;
	flipFeeLamports: bigint;
	totalFlips: bigint;
	sectionAllocationTokens: bigint;
	emissionDurationSeconds: bigint;
	windowSeconds: bigint;
	targetTokensPerWindow: bigint;
	startPriceLamports: bigint;
	minimumPriceLamports: bigint;
	maximumPriceLamports: bigint;
	startFloorPriceLamports: bigint;
	endFloorPriceLamports: bigint;
	changeDenominator: bigint;
	burstElasticity: bigint;
	ownerShareBasisPoints: number;
};

export type GameStateArgs = {
	gameIndex: number;
	status: number;
	bump: number;
	economyVersion: number;
	startsAt: number | bigint;
	nextSection: number;
	mintedSections: number;
	flipFeeLamports: number | bigint;
	totalFlips: number | bigint;
	sectionAllocationTokens: number | bigint;
	emissionDurationSeconds: number | bigint;
	windowSeconds: number | bigint;
	targetTokensPerWindow: number | bigint;
	startPriceLamports: number | bigint;
	minimumPriceLamports: number | bigint;
	maximumPriceLamports: number | bigint;
	startFloorPriceLamports: number | bigint;
	endFloorPriceLamports: number | bigint;
	changeDenominator: number | bigint;
	burstElasticity: number | bigint;
	ownerShareBasisPoints: number;
};

/** Gets the encoder for {@link GameStateArgs} account data. */
export function getGameStateEncoder(): FixedSizeEncoder<GameStateArgs> {
	return transformEncoder(
		getStructEncoder([
			["discriminator", getU8Encoder()],
			["migrationVersion", getU8Encoder()],
			["gameIndex", getU8Encoder()],
			["status", getU8Encoder()],
			["bump", getU8Encoder()],
			["economyVersion", getU8Encoder()],
			["startsAt", getI64Encoder()],
			["nextSection", getU16Encoder()],
			["mintedSections", getU16Encoder()],
			["flipFeeLamports", getU64Encoder()],
			["totalFlips", getU64Encoder()],
			["sectionAllocationTokens", getU64Encoder()],
			["emissionDurationSeconds", getU64Encoder()],
			["windowSeconds", getU64Encoder()],
			["targetTokensPerWindow", getU64Encoder()],
			["startPriceLamports", getU64Encoder()],
			["minimumPriceLamports", getU64Encoder()],
			["maximumPriceLamports", getU64Encoder()],
			["startFloorPriceLamports", getU64Encoder()],
			["endFloorPriceLamports", getU64Encoder()],
			["changeDenominator", getU64Encoder()],
			["burstElasticity", getU64Encoder()],
			["ownerShareBasisPoints", getU16Encoder()],
		]),
		(value) => ({ ...value, discriminator: 2, migrationVersion: 0 }),
	);
}

/** Gets the decoder for {@link GameState} account data. */
export function getGameStateDecoder(): FixedSizeDecoder<GameState> {
	return getStructDecoder([
		["discriminator", getPinaPodDiscriminatorDecoder(GAME_STATE_DISCRIMINATOR, getU8Decoder())],
		["migrationVersion", getPinaPodMigrationVersionDecoder(0, getU8Decoder())],
		["gameIndex", getU8Decoder()],
		["status", getU8Decoder()],
		["bump", getU8Decoder()],
		["economyVersion", getU8Decoder()],
		["startsAt", getI64Decoder()],
		["nextSection", getU16Decoder()],
		["mintedSections", getU16Decoder()],
		["flipFeeLamports", getU64Decoder()],
		["totalFlips", getU64Decoder()],
		["sectionAllocationTokens", getU64Decoder()],
		["emissionDurationSeconds", getU64Decoder()],
		["windowSeconds", getU64Decoder()],
		["targetTokensPerWindow", getU64Decoder()],
		["startPriceLamports", getU64Decoder()],
		["minimumPriceLamports", getU64Decoder()],
		["maximumPriceLamports", getU64Decoder()],
		["startFloorPriceLamports", getU64Decoder()],
		["endFloorPriceLamports", getU64Decoder()],
		["changeDenominator", getU64Decoder()],
		["burstElasticity", getU64Decoder()],
		["ownerShareBasisPoints", getU16Decoder()],
	]);
}

/** Gets the codec for {@link GameState} account data. */
export function getGameStateCodec(): FixedSizeCodec<GameStateArgs, GameState> {
	return combineCodec(getGameStateEncoder(), getGameStateDecoder());
}

export function decodeGameState<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress>,
): Account<GameState, TAddress>;
export function decodeGameState<TAddress extends string = string>(
	encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<GameState, TAddress>;
export function decodeGameState<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<GameState, TAddress> | MaybeAccount<GameState, TAddress> {
	return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getGameStateDecoder());
}

export async function fetchGameState<TAddress extends string = string>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<Account<GameState, TAddress>> {
	const maybeAccount = await fetchMaybeGameState(rpc, address, config);
	assertAccountExists(maybeAccount);
	return maybeAccount;
}

export async function fetchMaybeGameState<TAddress extends string = string>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<MaybeAccount<GameState, TAddress>> {
	const maybeAccount = await fetchEncodedAccount(rpc, address, config);
	return decodeGameState(maybeAccount);
}

export async function fetchAllGameState(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<Account<GameState>[]> {
	const maybeAccounts = await fetchAllMaybeGameState(rpc, addresses, config);
	assertAccountsExist(maybeAccounts);
	return maybeAccounts;
}

export async function fetchAllMaybeGameState(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<MaybeAccount<GameState>[]> {
	const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
	return maybeAccounts.map((maybeAccount) => decodeGameState(maybeAccount));
}

export async function fetchGameStateFromSeeds(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	seeds: GameSeeds,
	config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<GameState>> {
	const maybeAccount = await fetchMaybeGameStateFromSeeds(rpc, seeds, config);
	assertAccountExists(maybeAccount);
	return maybeAccount;
}

export async function fetchMaybeGameStateFromSeeds(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	seeds: GameSeeds,
	config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<GameState>> {
	const { programAddress, ...fetchConfig } = config;
	const [address] = await findGamePda(seeds, { programAddress });
	return await fetchMaybeGameState(rpc, address, fetchConfig);
}

/** The account schema version this client was generated from. */
export const GAME_STATE_MIGRATION_VERSION = 0;

/**
 * Cheap envelope check for a fetched `GameState` account: `true` only when the
 * bytes name this account's discriminator and a migration version older than
 * this client's schema. Those are exactly the accounts
 * {@link getMigrateInstruction} can bring current; every other mismatch is
 * reported by the decoder when the account is decoded.
 *
 * ```ts
 * const { data } = await fetchEncodedAccount(rpc, address);
 * if (gameStateNeedsMigration(data)) {
 * 	// Migrate first, then retry the instruction that failed.
 * 	await send(getMigrateInstruction({ gameState: address, payer }).make());
 * }
 * ```
 */
export function gameStateNeedsMigration(data: ReadonlyUint8Array): boolean {
	if (data.length < 2) {
		return false;
	}
	if (data[0] !== 2) {
		return false;
	}
	return data[1]! < 0;
}
