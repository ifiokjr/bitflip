/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
	type Account,
	type Address,
	assertAccountExists,
	assertAccountsExist,
	combineCodec,
	decodeAccount,
	type EncodedAccount,
	type FetchAccountConfig,
	type FetchAccountsConfig,
	fetchEncodedAccount,
	fetchEncodedAccounts,
	type FixedSizeCodec,
	type FixedSizeDecoder,
	type FixedSizeEncoder,
	getAddressDecoder,
	getAddressEncoder,
	getStructDecoder,
	getStructEncoder,
	getU16Decoder,
	getU16Encoder,
	getU32Decoder,
	getU32Encoder,
	getU64Decoder,
	getU64Encoder,
	getU8Decoder,
	getU8Encoder,
	type MaybeAccount,
	type MaybeEncodedAccount,
	type ReadonlyUint8Array,
	transformEncoder,
} from "@solana/kit";
import { findConfigPda } from "../pdas";
import { getPinaPodDiscriminatorDecoder, getPinaPodMigrationVersionDecoder } from "../pinaPodCodecs";

export const CONFIG_STATE_DISCRIMINATOR = 1;

export function getConfigStateDiscriminatorBytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(CONFIG_STATE_DISCRIMINATOR);
}

export const CONFIG_STATE_DISCRIMINATOR2 = 0;

export function getConfigStateDiscriminator2Bytes(): ReadonlyUint8Array {
	return getU8Encoder().encode(CONFIG_STATE_DISCRIMINATOR2);
}

export type ConfigState = {
	discriminator: number;
	migrationVersion: number;
	version: number;
	authority: Address;
	pendingAuthority: Address;
	treasury: Address;
	collectionAuthority: Address;
	bitMint: Address;
	bitReserve: Address;
	claimPriceLamports: bigint;
	flipFeeLamports: bigint;
	minimumFlipFeeLamports: bigint;
	maximumFlipFeeLamports: bigint;
	unlockIntervalSeconds: number;
	earlyUnlockFlips: number;
	gameCount: number;
	bump: number;
};

export type ConfigStateArgs = {
	version: number;
	authority: Address;
	pendingAuthority: Address;
	treasury: Address;
	collectionAuthority: Address;
	bitMint: Address;
	bitReserve: Address;
	claimPriceLamports: number | bigint;
	flipFeeLamports: number | bigint;
	minimumFlipFeeLamports: number | bigint;
	maximumFlipFeeLamports: number | bigint;
	unlockIntervalSeconds: number;
	earlyUnlockFlips: number;
	gameCount: number;
	bump: number;
};

/** Gets the encoder for {@link ConfigStateArgs} account data. */
export function getConfigStateEncoder(): FixedSizeEncoder<ConfigStateArgs> {
	return transformEncoder(
		getStructEncoder([
			["discriminator", getU8Encoder()],
			["migrationVersion", getU8Encoder()],
			["version", getU8Encoder()],
			["authority", getAddressEncoder()],
			["pendingAuthority", getAddressEncoder()],
			["treasury", getAddressEncoder()],
			["collectionAuthority", getAddressEncoder()],
			["bitMint", getAddressEncoder()],
			["bitReserve", getAddressEncoder()],
			["claimPriceLamports", getU64Encoder()],
			["flipFeeLamports", getU64Encoder()],
			["minimumFlipFeeLamports", getU64Encoder()],
			["maximumFlipFeeLamports", getU64Encoder()],
			["unlockIntervalSeconds", getU32Encoder()],
			["earlyUnlockFlips", getU32Encoder()],
			["gameCount", getU16Encoder()],
			["bump", getU8Encoder()],
		]),
		(value) => ({ ...value, discriminator: 1, migrationVersion: 0 }),
	);
}

/** Gets the decoder for {@link ConfigState} account data. */
export function getConfigStateDecoder(): FixedSizeDecoder<ConfigState> {
	return getStructDecoder([
		["discriminator", getPinaPodDiscriminatorDecoder(CONFIG_STATE_DISCRIMINATOR, getU8Decoder())],
		["migrationVersion", getPinaPodMigrationVersionDecoder(0, getU8Decoder())],
		["version", getU8Decoder()],
		["authority", getAddressDecoder()],
		["pendingAuthority", getAddressDecoder()],
		["treasury", getAddressDecoder()],
		["collectionAuthority", getAddressDecoder()],
		["bitMint", getAddressDecoder()],
		["bitReserve", getAddressDecoder()],
		["claimPriceLamports", getU64Decoder()],
		["flipFeeLamports", getU64Decoder()],
		["minimumFlipFeeLamports", getU64Decoder()],
		["maximumFlipFeeLamports", getU64Decoder()],
		["unlockIntervalSeconds", getU32Decoder()],
		["earlyUnlockFlips", getU32Decoder()],
		["gameCount", getU16Decoder()],
		["bump", getU8Decoder()],
	]);
}

/** Gets the codec for {@link ConfigState} account data. */
export function getConfigStateCodec(): FixedSizeCodec<ConfigStateArgs, ConfigState> {
	return combineCodec(getConfigStateEncoder(), getConfigStateDecoder());
}

export function decodeConfigState<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress>,
): Account<ConfigState, TAddress>;
export function decodeConfigState<TAddress extends string = string>(
	encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<ConfigState, TAddress>;
export function decodeConfigState<TAddress extends string = string>(
	encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<ConfigState, TAddress> | MaybeAccount<ConfigState, TAddress> {
	return decodeAccount(encodedAccount as MaybeEncodedAccount<TAddress>, getConfigStateDecoder());
}

export async function fetchConfigState<TAddress extends string = string>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<Account<ConfigState, TAddress>> {
	const maybeAccount = await fetchMaybeConfigState(rpc, address, config);
	assertAccountExists(maybeAccount);
	return maybeAccount;
}

export async function fetchMaybeConfigState<TAddress extends string = string>(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	address: Address<TAddress>,
	config?: FetchAccountConfig,
): Promise<MaybeAccount<ConfigState, TAddress>> {
	const maybeAccount = await fetchEncodedAccount(rpc, address, config);
	return decodeConfigState(maybeAccount);
}

export async function fetchAllConfigState(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<Account<ConfigState>[]> {
	const maybeAccounts = await fetchAllMaybeConfigState(rpc, addresses, config);
	assertAccountsExist(maybeAccounts);
	return maybeAccounts;
}

export async function fetchAllMaybeConfigState(
	rpc: Parameters<typeof fetchEncodedAccounts>[0],
	addresses: Array<Address>,
	config?: FetchAccountsConfig,
): Promise<MaybeAccount<ConfigState>[]> {
	const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
	return maybeAccounts.map((maybeAccount) => decodeConfigState(maybeAccount));
}

export async function fetchConfigStateFromSeeds(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<Account<ConfigState>> {
	const maybeAccount = await fetchMaybeConfigStateFromSeeds(rpc, config);
	assertAccountExists(maybeAccount);
	return maybeAccount;
}

export async function fetchMaybeConfigStateFromSeeds(
	rpc: Parameters<typeof fetchEncodedAccount>[0],
	config: FetchAccountConfig & { programAddress?: Address } = {},
): Promise<MaybeAccount<ConfigState>> {
	const { programAddress, ...fetchConfig } = config;
	const [address] = await findConfigPda({ programAddress });
	return await fetchMaybeConfigState(rpc, address, fetchConfig);
}

/** The account schema version this client was generated from. */
export const CONFIG_STATE_MIGRATION_VERSION = 0;

/**
 * Cheap envelope check for a fetched `ConfigState` account: `true` only when the
 * bytes name this account's discriminator and a migration version older than
 * this client's schema. Those are exactly the accounts
 * {@link getMigrateInstruction} can bring current; every other mismatch is
 * reported by the decoder when the account is decoded.
 *
 * ```ts
 * const { data } = await fetchEncodedAccount(rpc, address);
 * if (configStateNeedsMigration(data)) {
 * 	// Migrate first, then retry the instruction that failed.
 * 	await send(getMigrateInstruction({ configState: address, payer }).make());
 * }
 * ```
 */
export function configStateNeedsMigration(data: ReadonlyUint8Array): boolean {
	if (data.length < 2) {
		return false;
	}
	if (data[0] !== 1) {
		return false;
	}
	return data[1]! < 0;
}
