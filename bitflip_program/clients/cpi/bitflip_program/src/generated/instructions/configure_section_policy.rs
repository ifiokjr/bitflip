//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]
#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramError;
use pina::ProgramResult;
use pina::Signer;

use crate::ProgramAccount;

/// CPI call for the `configure_section_policy` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct ConfigureSectionPolicy<'account> {
	/// CPI account `owner`.
	/// Required privileges: read-only and signer.
	pub owner: &'account AccountView,

	/// CPI account `section`.
	/// Required privileges: writable.
	pub section: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `configure_section_policy`.
	pub ix: ConfigureSectionPolicyIx,
}

/// Instruction arguments for the `configure_section_policy` CPI call.
#[derive(Clone, Copy, Debug)]
pub struct ConfigureSectionPolicyIx {
	/// Instruction argument `gameIndex`.
	pub game_index: u8,

	/// Instruction argument `sectionIndex`.
	pub section_index: u8,

	/// Instruction argument `expectedPolicyVersion`.
	pub expected_policy_version: u64,

	/// Instruction argument `mode`.
	pub mode: u8,

	/// Instruction argument `paletteId`.
	pub palette_id: u8,

	/// Instruction argument `rewardPolicy`.
	pub reward_policy: u8,

	/// Instruction argument `startsAt`.
	pub starts_at: i64,

	/// Instruction argument `endsAt`.
	pub ends_at: i64,

	/// Instruction argument `entryPriceTokens`.
	pub entry_price_tokens: u64,

	/// Instruction argument `rewardPerActionTokens`.
	pub reward_per_action_tokens: u64,

	/// Instruction argument `rulesDigest`.
	pub rules_digest: [u8; 32],
}

impl ConfigureSectionPolicyIx {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 79;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 79], ProgramError> {
		let mut data = [0u8; 79];
		data[..2].copy_from_slice(&CONFIGURE_SECTION_POLICY_DISCRIMINATOR);
		data[2..3].copy_from_slice(&self.game_index.to_le_bytes());
		data[3..4].copy_from_slice(&self.section_index.to_le_bytes());
		data[4..12].copy_from_slice(&self.expected_policy_version.to_le_bytes());
		data[12..13].copy_from_slice(&self.mode.to_le_bytes());
		data[13..14].copy_from_slice(&self.palette_id.to_le_bytes());
		data[14..15].copy_from_slice(&self.reward_policy.to_le_bytes());
		data[15..23].copy_from_slice(&self.starts_at.to_le_bytes());
		data[23..31].copy_from_slice(&self.ends_at.to_le_bytes());
		data[31..39].copy_from_slice(&self.entry_price_tokens.to_le_bytes());
		data[39..47].copy_from_slice(&self.reward_per_action_tokens.to_le_bytes());
		data[47..79].copy_from_slice(&self.rules_digest);

		Ok(data)
	}
}

impl<'account> ConfigureSectionPolicy<'account> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 2] = [
			CpiHandle::readonly_signer(self.owner),
			CpiHandle::writable(self.section)?,
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const CONFIGURE_SECTION_POLICY_DISCRIMINATOR: [u8; 2] = [16, 0];
