//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]
#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::Address;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramError;
use pina::ProgramResult;
use pina::Signer;

use crate::ProgramAccount;

/// CPI call for the `update_config` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct UpdateConfig<'account, 'argument> {
	/// CPI account `authority`.
	/// Required privileges: read-only and signer.
	pub authority: &'account AccountView,

	/// CPI account `config`.
	/// Required privileges: writable.
	pub config: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `update_config`.
	pub ix: UpdateConfigIx<'argument>,
}

/// Instruction arguments for the `update_config` CPI call.
#[derive(Clone, Copy, Debug)]
pub struct UpdateConfigIx<'argument> {
	/// Instruction argument `treasury`.
	pub treasury: &'argument Address,

	/// Instruction argument `collectionAuthority`.
	pub collection_authority: &'argument Address,

	/// Instruction argument `claimPriceLamports`.
	pub claim_price_lamports: u64,

	/// Instruction argument `flipFeeLamports`.
	pub flip_fee_lamports: u64,

	/// Instruction argument `minimumFlipFeeLamports`.
	pub minimum_flip_fee_lamports: u64,

	/// Instruction argument `maximumFlipFeeLamports`.
	pub maximum_flip_fee_lamports: u64,

	/// Instruction argument `unlockIntervalSeconds`.
	pub unlock_interval_seconds: u32,

	/// Instruction argument `earlyUnlockFlips`.
	pub early_unlock_flips: u32,
}

impl<'argument> UpdateConfigIx<'argument> {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 106;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 106], ProgramError> {
		let mut data = [0u8; 106];
		data[..2].copy_from_slice(&UPDATE_CONFIG_DISCRIMINATOR);
		data[2..34].copy_from_slice(self.treasury.as_ref());
		data[34..66].copy_from_slice(self.collection_authority.as_ref());
		data[66..74].copy_from_slice(&self.claim_price_lamports.to_le_bytes());
		data[74..82].copy_from_slice(&self.flip_fee_lamports.to_le_bytes());
		data[82..90].copy_from_slice(&self.minimum_flip_fee_lamports.to_le_bytes());
		data[90..98].copy_from_slice(&self.maximum_flip_fee_lamports.to_le_bytes());
		data[98..102].copy_from_slice(&self.unlock_interval_seconds.to_le_bytes());
		data[102..106].copy_from_slice(&self.early_unlock_flips.to_le_bytes());

		Ok(data)
	}
}

impl<'account, 'argument> UpdateConfig<'account, 'argument> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 2] = [
			CpiHandle::readonly_signer(self.authority),
			CpiHandle::writable(self.config)?,
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const UPDATE_CONFIG_DISCRIMINATOR: [u8; 2] = [1, 0];
