//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]
#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::Address;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramError;
use pina::ProgramResult;
use pina::Signer;

use crate::ProgramAccount;

/// CPI call for the `record_section_mint` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct RecordSectionMint<'account, 'argument> {
	/// CPI account `collectionAuthority`.
	/// Required privileges: read-only and signer.
	pub collection_authority: &'account AccountView,

	/// CPI account `config`.
	/// Required privileges: read-only.
	pub config: &'account AccountView,

	/// CPI account `game`.
	/// Required privileges: writable.
	pub game: &'account AccountView,

	/// CPI account `section`.
	/// Required privileges: writable.
	pub section: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `record_section_mint`.
	pub ix: RecordSectionMintIx<'argument>,
}

/// Instruction arguments for the `record_section_mint` CPI call.
#[derive(Clone, Copy, Debug)]
pub struct RecordSectionMintIx<'argument> {
	/// Instruction argument `gameIndex`.
	pub game_index: u8,

	/// Instruction argument `sectionIndex`.
	pub section_index: u8,

	/// Instruction argument `expectedOwner`.
	pub expected_owner: &'argument Address,

	/// Instruction argument `assetId`.
	pub asset_id: &'argument Address,

	/// Instruction argument `merkleTree`.
	pub merkle_tree: &'argument Address,

	/// Instruction argument `leafIndex`.
	pub leaf_index: u32,
}

impl<'argument> RecordSectionMintIx<'argument> {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 104;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 104], ProgramError> {
		let mut data = [0u8; 104];
		data[..2].copy_from_slice(&RECORD_SECTION_MINT_DISCRIMINATOR);
		data[2..3].copy_from_slice(&self.game_index.to_le_bytes());
		data[3..4].copy_from_slice(&self.section_index.to_le_bytes());
		data[4..36].copy_from_slice(self.expected_owner.as_ref());
		data[36..68].copy_from_slice(self.asset_id.as_ref());
		data[68..100].copy_from_slice(self.merkle_tree.as_ref());
		data[100..104].copy_from_slice(&self.leaf_index.to_le_bytes());

		Ok(data)
	}
}

impl<'account, 'argument> RecordSectionMint<'account, 'argument> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 4] = [
			CpiHandle::readonly_signer(self.collection_authority),
			CpiHandle::readonly(self.config),
			CpiHandle::writable(self.game)?,
			CpiHandle::writable(self.section)?,
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const RECORD_SECTION_MINT_DISCRIMINATOR: [u8; 2] = [8, 0];
