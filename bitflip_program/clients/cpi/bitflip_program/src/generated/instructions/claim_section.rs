//! This code was AUTOGENERATED by `pina_cpi_renderer`.
//! Please DO NOT EDIT THIS FILE directly.
//! Regenerate it from Codama IDLs instead.
#![allow(
	clippy::doc_lazy_continuation,
	clippy::empty_line_after_doc_comments,
	clippy::too_many_arguments
)]
#![allow(rustdoc::broken_intra_doc_links)]

use pina::AccountView;
use pina::CpiContext;
use pina::CpiHandle;
use pina::ProgramError;
use pina::ProgramResult;
use pina::Signer;

use crate::ProgramAccount;

/// CPI call for the `claim_section` instruction.
#[derive(Clone, Copy, Debug)]
#[must_use = "the CPI has no effect until invoke or invoke_signed is called"]
pub struct ClaimSection<'account> {
	/// CPI account `owner`.
	/// Required privileges: writable and signer.
	pub owner: &'account AccountView,

	/// CPI account `config`.
	/// Required privileges: read-only.
	pub config: &'account AccountView,

	/// CPI account `game`.
	/// Required privileges: writable.
	pub game: &'account AccountView,

	/// CPI account `previousSection`.
	/// Required privileges: read-only.
	pub previous_section: &'account AccountView,

	/// CPI account `section`.
	/// Required privileges: writable.
	pub section: &'account AccountView,

	/// CPI account `treasury`.
	/// Required privileges: writable.
	pub treasury: &'account AccountView,

	/// CPI account `systemProgram`.
	/// Required privileges: read-only.
	pub system_program: &'account AccountView,

	/// Instruction arguments encoded and sent as CPI data for `claim_section`.
	pub ix: ClaimSectionIx,
}

/// Instruction arguments for the `claim_section` CPI call.
#[derive(Clone, Copy, Debug)]
pub struct ClaimSectionIx {
	/// Instruction argument `gameIndex`.
	pub game_index: u8,

	/// Instruction argument `sectionIndex`.
	pub section_index: u8,

	/// Instruction argument `bump`.
	pub bump: u8,

	/// Instruction argument `maximumPriceLamports`.
	pub maximum_price_lamports: u64,
}

impl ClaimSectionIx {
	/// Number of bytes in the encoded instruction, including its discriminator.
	pub const LEN: usize = 13;

	/// Encodes the discriminator and instruction arguments for CPI.
	#[inline(always)]
	pub fn to_bytes(&self) -> Result<[u8; 13], ProgramError> {
		let mut data = [0u8; 13];
		data[..2].copy_from_slice(&CLAIM_SECTION_DISCRIMINATOR);
		data[2..3].copy_from_slice(&self.game_index.to_le_bytes());
		data[3..4].copy_from_slice(&self.section_index.to_le_bytes());
		data[4..5].copy_from_slice(&self.bump.to_le_bytes());
		data[5..13].copy_from_slice(&self.maximum_price_lamports.to_le_bytes());

		Ok(data)
	}
}

impl<'account> ClaimSection<'account> {
	/// Invokes the instruction with no PDA seeds.
	#[inline(always)]
	pub fn invoke(&self, program: &ProgramAccount<'_>) -> ProgramResult {
		self.invoke_signed(program, &[])
	}

	/// Invokes the instruction, signing with the provided PDA seeds.
	#[inline(always)]
	pub fn invoke_signed(
		&self,
		program: &ProgramAccount<'_>,
		signers: &[Signer<'_, '_>],
	) -> ProgramResult {
		let accounts: [CpiHandle<'_>; 7] = [
			CpiHandle::writable_signer(self.owner)?,
			CpiHandle::readonly(self.config),
			CpiHandle::writable(self.game)?,
			CpiHandle::readonly(self.previous_section),
			CpiHandle::writable(self.section)?,
			CpiHandle::writable(self.treasury)?,
			CpiHandle::readonly(self.system_program),
		];
		let data = self.ix.to_bytes()?;
		let context = CpiContext::new(*program, accounts);

		context.invoke_signed(&data, signers)
	}
}

const CLAIM_SECTION_DISCRIMINATOR: [u8; 2] = [5, 0];
