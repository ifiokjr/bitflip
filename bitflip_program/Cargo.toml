[package]
name = "bitflip_program"
version.workspace = true
authors.workspace = true
edition.workspace = true
homepage.workspace = true
license.workspace = true
publish = false
repository.workspace = true
rust-version.workspace = true
description = "The no_std Pina program powering the Bitflip pixel canvas"

[lib]
crate-type = ["cdylib", "lib"]

[features]
bpf-entrypoint = []
# Never enable this in a deployable build. It exists only so the isolated SBF
# suite can sign authority-gated instructions with a public test key.
sbf-test-authority = []

[dependencies]
pina = { workspace = true, default-features = false, features = ["derive", "logs"] }

[lints]
workspace = true

[package.metadata.solana]
program-id = "5AuNvfV9Xi9gskJpW2qQJndQkFcwbWNV6fjaf2VvuEcM"
